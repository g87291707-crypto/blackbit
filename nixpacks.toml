[providers]
python = "3.11"

[phases.setup]
nixPackages = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.start]
cmds = ["cd backend && python app.py"]
